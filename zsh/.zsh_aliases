# brew
alias upgrade="brew upgrade"
alias remove="brew remove"

# nvim
alias vi="nvim"

# cat to bat
alias cat="bat"

# ls
alias ls="eza"
alias l="ls"

alias la="ls -a"
alias ll="ls -alF"
alias lt="ls --tree"

# interactive
alias mv="mv -i"
alias rm="rm -i"

# git
alias gs="git status"
alias gcc="git commit"
alias gce="git commit --amend"
alias gco="git checkout"
alias gcl="git clone"
alias gl="git log --graph --all --oneline"

# python
alias python="python3"
alias crl="cd /Users/monterml/Projects/crl/"
alias uvr="uv run"
alias uvrp="uv run python"
alias uvrm="uv run python -m"
alias uvi="uv run nvim"
alias uvim="uv run nvim"

# ssh
alias ssht='ssh -t'
alias bindport='bash /Users/monterml/.local/bin/scripts/bindport'

# ssh with kitty so configs work on remote
[[ "$TERM" == "xterm-kitty" ]] && alias ssh="kitty +kitten ssh"
